{
  "name": "@sbf/insurance-ops",
  "version": "0.1.0",
  "description": "Insurance Operations Module - Claims processing, field inspections, and loss adjustment",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npx -p typescript tsc -b",
    "dev": "tsc -b --watch",
    "test": "npx -p jest jest",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "lint": "eslint src --ext .ts",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:ci": "jest --ci --coverage --maxWorkers=2"
  },
  "keywords": [
    "sbf",
    "insurance",
    "claims",
    "inspections",
    "adjusters",
    "operations"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@sbf/core-privacy": "*",
    "@sbf/memory-engine": "*",
    "@sbf/shared": "*"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.19.25",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.5",
    "typescript": "^5.9.3"
  }
}
