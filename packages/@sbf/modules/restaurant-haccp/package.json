{
  "name": "@sbf/modules-restaurant-haccp",
  "version": "1.0.0",
  "description": "Restaurant HACCP and food safety compliance module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npx -p typescript tsc || exit 0",
    "test": "npx -p jest jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:ci": "jest --ci --coverage --maxWorkers=2",
    "clean": "rimraf dist"
  },
  "keywords": [
    "sbf",
    "module",
    "restaurant",
    "haccp",
    "food-safety",
    "compliance"
  ],
  "author": "Second Brain Foundation",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
